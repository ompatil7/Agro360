doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Insurance Assignment Update
    style.
      :root {
        --field-green: #4CAF50;
        --earth-brown: #6D4C41;
        --sun-gold: #FFC107;
        --sky-blue: #87CEEB;
      }

      body {
        margin: 0;
        padding: 0;
        background: #f5f5f5;
        font-family: 'Helvetica Neue', Arial, sans-serif;
      }

      .container {
        max-width: 700px;
        margin: 2rem auto;
        background: white;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      }

      .header {
        background: linear-gradient(135deg, var(--field-green) 0%, var(--earth-brown) 100%);
        padding: 2.5rem;
        text-align: center;
        position: relative;
      }

      .header::after {
        content: '';
        position: absolute;
        bottom: -30px;
        left: 0;
        width: 100%;
        height: 60px;
        background: url('data:image/svg+xml;utf8,<svg viewBox="0 0 100 10" xmlns="http://www.w3.org/2000/svg"><path d="M0 10 Q 25 5 50 10 T 100 10 L 100 0 L 0 0" fill="white"/></svg>');
      }

      h1 {
        color: white;
        margin: 1rem 0;
        font-size: 2.2em;
        text-shadow: 0 2px 4px rgba(0,0,0,0.2);
      }

      .content {
        padding: 3rem 2rem;
      }

      .status-card {
        background: #f8f9fa;
        padding: 2rem;
        border-radius: 10px;
        border-left: 4px solid var(--field-green);
        margin-bottom: 2rem;
      }

      .timeline {
        display: flex;
        justify-content: space-between;
        background: #fff8e1;
        padding: 1.5rem;
        border-radius: 10px;
        margin: 2rem 0;
      }

      .cta-button {
        display: inline-block;
        padding: 1rem 2rem;
        background: var(--sun-gold);
        color: #333 !important;
        text-decoration: none;
        border-radius: 25px;
        font-weight: bold;
        transition: transform 0.3s ease;
      }

      .cta-button:hover {
        transform: translateY(-2px);
      }

      .footer {
        background: var(--earth-brown);
        color: white;
        padding: 1.5rem;
        text-align: center;
        margin-top: 2rem;
      }

  body
    .container
      .header
        h1 🛡️ Insurance Assignment Update
        p(style="color: rgba(255,255,255,0.9)") Your Farming Security Partner

      .content
        .status-card
          h2(style="margin-bottom: 1rem") 🌾 Hello #{firstName},
          p Your insurance application status has been updated:
          p Status: 
            span(style="font-weight: bold; color: var(--field-green)") #{assignment.status.toUpperCase()}
          p Application Date: #{assignment.applicationDate.toDateString()}

        if assignment.visitDate
          .timeline
            div
              h4 📅 Scheduled Visit
              p #{assignment.visitDate.toDateString()}
            div
              h4 📍 Location
              p #{assignment.region.district}, #{assignment.region.state}

        .status-card
          h3(style="margin-bottom: 1rem") 📋 Policy Details
          p Policy: #{insurancePolicy.name}
          p Coverage Area: #{assignment.region.district}, #{assignment.region.state}
          p Application ID: #{assignment._id}

        center
          a.cta-button(href="#") View Full Details
          p(style="margin-top: 1rem") Need assistance? Contact our support team

        .footer
          p 🌱 Protecting Farmers Since 2023
          p 📞 1800-AGRI-HELP | 📧 support@cropsecure.in
          p Follow us: 🌾 Instagram | 📘 Facebook | 🐦 Twitter